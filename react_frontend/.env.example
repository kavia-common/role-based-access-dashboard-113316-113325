# Required environment variables for the React frontend
# These will be read at build time by Create React App (prefix REACT_APP_).
REACT_APP_SUPABASE_URL=https://YOUR-SUPABASE-PROJECT.supabase.co
REACT_APP_SUPABASE_KEY=YOUR-ANON-PUBLIC-KEY

# Optional: Explicitly set the site URL used to build absolute redirect links for Supabase
# If not provided, the app will fallback to window.location.origin at runtime.
# Set this to the deployed site URL (e.g., https://yourapp.example.com)
# REACT_APP_SITE_URL=https://yourapp.example.com
